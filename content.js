window.addEventListener('load', function() {
    const elements = document.querySelectorAll('.viewer code');
    elements.forEach(element => {
      element.style.backgroundColor = 'var(--gray-750)';
    });
  });
  